package test;
import java.util.ArrayList;

import org.junit.Assert;
import org.junit.Test;
import modele.Restaurant;
import modele.Serveur;
import modele.Franchise;

public class TestRestaurant {
/*	@SuppressWarnings("deprecation")
	@Test
	public void RestaurantChiffreAffaires() {
		ArrayList<Serveur> serveurs = new ArrayList<Serveur>();
		Serveur serveur1 = new Serveur("john",100);
		serveur1.PrendreCommande(10.3);
		Serveur serveur2 = new Serveur("yassine",100);
		serveur2.PrendreCommande(10.3);
		Serveur serveur3 = new Serveur("aymane",100);
		serveur3.PrendreCommande(10.3);
		serveurs.add(serveur1);
		serveurs.add(serveur2);
		serveurs.add(serveur3);
		ArrayList<Restaurant> restaurants = new ArrayList<Restaurant>();
		Restaurant restaurant1=new Restaurant( serveurs) ; 
		Restaurant restaurant2=new Restaurant( serveurs) ; 
		Restaurant restaurant3=new Restaurant( serveurs) ; 
		Restaurant restaurant4=new Restaurant( serveurs) ; 
		restaurants.add(restaurant1);
		restaurants.add(restaurant2);
		restaurants.add(restaurant3);
		restaurants.add(restaurant4);
		Franchise franchise=new Franchise(restaurants);
		double result=10.3*3;
		double resultat=10.3*3*4;
		Assert.assertEquals(result, restaurant1.ChiffreAffaires(),result);
		Assert.assertEquals(resultat, franchise.ChiffreAffaires(),resultat);
	}*/
	
		




}
